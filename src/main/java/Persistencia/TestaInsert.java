package Persistencia;

import Entidades.Perfil;
import Entidades.Usuario;
import java.sql.SQLException;
import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.sql.Date;

public class TestaInsert {

    public static void main(String[] args) throws SQLException, ParseException {

        /*
        Perfil perfil = new Perfil();
        perfil.setCodigo(201);
        perfil.setNome("ROOT");
        perfil.setDescricao("Super Usuário top");

        PerfilDAO perfilDao = new PerfilDAO();

        // perfilDao.deletar(perfil);
        //  perfilDao.salvar(perfil);
        Perfil perfilRetornadoDoBancoDeDado = (Perfil) perfilDao.buscarPorId(201);

        //  System.out.println(perfilRetornadoDoBancoDeDado.getNome());
        //  System.out.println(perfilRetornadoDoBancoDeDado.getDescricao());
        perfil.setNome("Root_3");
        perfil.setDescricao("Super Usuário");

          perfilDao.atualizar(perfil);
          perfilRetornadoDoBancoDeDado = (Perfil) perfilDao.buscarPorId(201);
          System.out.println(perfilRetornadoDoBancoDeDado.getNome());
          System.out.println(perfilRetornadoDoBancoDeDado.getDescricao());
        */  
          
        
    }

}
